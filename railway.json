{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd QuestPdfApi && dotnet restore && dotnet build -c Release && dotnet publish -c Release -o ./publish"
  },
  "deploy": {
    "startCommand": "cd QuestPdfApi/publish && dotnet QuestPdfApi.dll --urls http://0.0.0.0:$PORT",
    "healthcheckPath": "/api/pdf/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 