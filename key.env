# Email Configuration
# 請在 Gmail 設定中開啟兩步驟驗證，然後生成應用程式密碼
# 設定路徑：Gmail > 管理您的 Google 帳戶 > 安全性 > 兩步驟驗證 > 應用程式密碼
EMAIL_USER=tyouxipindao@gmail.com
EMAIL_PASS=ecao uqoa lpxx sswi

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_secret_here

# PDF 生成配置
# 設為 true 使用後端 PDF 生成（Puppeteer）
# 設為 false 使用前端 PDF 生成（html2canvas + jsPDF）
NEXT_PUBLIC_USE_BACKEND_PDF=true 